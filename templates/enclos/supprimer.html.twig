{% extends 'base.html.twig' %}

{% block title %}Supprimer un enclos{% endblock %}

{% block body %}
    <div class="container">
        <h1>Supprimer l'enclos'' <i>{{ enclos.nom }}</i></h1>
        <p>L'enclos de cette espace sera supprimé et les animaux à l'interieur.</p>
        {{ form(formulaire) }}
        <a href="{{ path("app_categories") }}" class="btn btn-primary">Retour</a>
    </div>
{% endblock %}